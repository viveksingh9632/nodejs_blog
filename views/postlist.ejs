<%- include('layout/header') %>
<div class="container">
    <div class="row my-4">
        <div class="col-lg-12">
            <% if (message) { %>
                <div class="alert alert-dismissible fade show alert-<%= message.type %>" role="alert">
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    <strong><%= message.message %></strong>
                </div>
            <% } %>
            <div class="table-responsive">
                <table class="table table-striped text-center">
                    <thead>
                        <tr class="table-light">
                            <th>Name</th>
                            <th>Image</th>
                            <th>Slug</th>
                            <th>Content</th>
                            <th>Password</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% posts.forEach(function(post, index) { %>
                            <tr>
                                <td><%= post.name %></td>
                                <td><img src="<%= post.image %>" width="50" class="img-thumbnail"></td>
                                <td><%= post.slug %></td>
                                <td><%= post.content %></td>
                                <td><%= post.password %></td>
                                <td>
                                    <a href="/edit-post/<%= post._id %>" class="text-success" title="Edit">
                                        <i class="fas fa-edit fa-lg mx-1"></i>
                                    </a>
                                    <a href="/delete-post/<%= post._id %>" class="text-danger" title="Delete">
                                        <i class="fas fa-trash-alt fa-lg mx-1"></i>
                                    </a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<%- include('layout/footer') %>
